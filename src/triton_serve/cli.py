def main():
    """Entry point for the application script.

    Intentionally empty: reserved for the CLI, tracked separately. The `[project.scripts]` entry in
    pyproject.toml stays commented out until then.
    """
